class Solution(object):
    def isAnagram(self, s, t):
        if len(s)==len(t):
            sl=sorted(list(s))
            tl=sorted(list(t))
            if sl==tl:
                return True
            else :
                return False
        else:
            return False 
